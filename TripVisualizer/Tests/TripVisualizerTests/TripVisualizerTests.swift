import XCTest
@testable import TripVisualizer

/// Root test class to verify test target setup
final class TripVisualizerTests: XCTestCase {

    func testTargetSetup() {
        // Verify test target is properly configured
        XCTAssertTrue(true, "Test target is configured correctly")
    }
}
